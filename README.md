## Vitest VSCode Coverage Workspace Issue

Reproduction of VSCode coverage not working:

- `pnpm test:coverage` outputs coverage fine
- Using VSCode run tests with coverage passes but no coverage is reported
